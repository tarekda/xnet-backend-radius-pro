# Allow STOMP/WebSocket clients that reach RabbitMQ via Docker NAT / Cloudflare
# (source IP is the bridge gateway, not 127.0.0.1). Without this, guest login fails
# and the frontend shows "Unable to establish WebSocket connection".
loopback_users = none
